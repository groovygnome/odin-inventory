<!DOCTYPE html>
<html>
    <head>
        <title>The Arsenal</title>
    </head>
    <body>
        <h1>The Arsenal</h1>
        <select name='series' id='series'>
            <option value=''>Filter to series...</option>
            <% inventory.forEach((weapon) => { %>
                <option value=<%= weapon.series %>><%= weapon.series %></option>
            <% }); %>
        </select>
        <%- include('./partials/error.ejs') %>
        <ul>
            <% inventory.forEach((weapon) => { %>
                <li>
                    <%- include('./partials/weapon.ejs', {weapon: weapon}) %>
                </li>
            <% }); %>
        </ul>

        <a href='/new'>Add a New Weapon</a>
    </body>
</html>
